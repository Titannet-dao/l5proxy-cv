folders:
    meta: base data type and interface type

    local: handle tun device and user space netstack, base on xjasonlyu/tun2socks

    remote: handle websocket tunnel to remote server

    xy: bridge 'up' and 'tun', construct a proxy system

    android: for android build and exports
